#include <iostream>
using namespace std;

struct ListNode {
	int val;
	ListNode *next;
	ListNode(int x) : val(x), next(NULL) {}
};


class Solution {
public:
	ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {
		ListNode dummy(0);
		ListNode* tail = &dummy;
	}
};

int main(int argc, char *argv[]) {
	Solution ans;
	ans.mergeTwoLists(ListNode *l1, ListNode *l2)
}